#对注册中心的配置
server:
  port: 8761
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    # 不注册它自己
    register-with-eureka: false
    #是否从其他的注册中心导入数据
    fetch-registry: false